# Server Port
server:
  port: 8081

# Logging configuration
logging:
  level:
    root: INFO

spring:
  # MongoDB
  data:
    mongodb:
      username: ricsanfre
      password: password
      host: localhost
      port: 27017
      database: product
      authentication-database: admin